touch ignore =>used to ignore unecessary files
git config --global user.name Saud
git config --global user.email saudahmed43105@gmail.com
git init
git add .
git commit -m "initial commit"
git status =>just to see
ssh-keygen -t ed25519 -C "your_email@example.com"
cat <path to id-rsa.pub>
git remote add origin <ssh url.git>
git pus origin master
